library(haven)
salesData <- read_sav("examples/salesData.sav")
View(salesData)
summary(salesdata)
summary(salesData)
library(haven)
salesData <- read_sav("examples/salesData.sav")
View(salesData)
summary(salesData)
n <-  15
n <-  15
n <- 25
n <- 25
n <- 30
n <- 25
n <- 30
number of participants <- 100
number_of_participants <- 100
number_of_participants
names <- c("mary", "john", "mike")
names
numbers <- c(1,2,3)
numbers
mean_numbers <- mean(numbers)
numbers <- c(1,2,3,4,5,6)
mean_numbers <- mean(numbers)
c()
?c()
numbers <- c(1,2,3,4,5,6)
mean(numbers)
names <- c("jim", "tom", "amy")
mean(names)
?mean()
x <- c(0:10, 50)
x
x <- c(0:10, 50, 78, 99)
x
meanx <- mean(x, trim = 0.1, na.rm = TRUE)
library(readr)
album_sales <- read_csv("datasets/album_sales.csv")
View(album_sales)
View(album_sales)
album_sales$Genre  <-  as.factor(album_sales$Genre)
album_sales %>% filter(Attract > 1) %>% filter(Genre == "Pop" | Genre == "Metal" | Genre == "HipHop")
# Change genre to a factor variable
library(tidyverse)
album_sales %>% filter(Attract > 1) %>% filter(Genre == "Pop" | Genre == "Metal" | Genre == "HipHop")
filtered_data <- album_sales %>% filter(Attract > 1) %>% filter(Genre == "Pop" | Genre == "Metal" | Genre == "HipHop")
album_sales %>% filter(Attract > 5 & Genre == "Metal")
album_sales %>% filter(Attract > 7 & Genre == "Metal")
album_sales %>% filter(Genre != "Country")
album_sales %>%
group_by(Genre) %>%
summarise(mean_sales = mean(Sales)      )
album_sales %>%
group_by(Genre) %>%
summarise(mean_sales = mean(Sales), sd_sales = sd(Sales), z_sales = mean(Sales) - Sales      )
album_sales %>%
group_by(Genre) %>%
summarise(mean_sales = mean(Sales), sd_sales = sd(Sales)  )
data_summary <- album_sales %>%
group_by(Genre) %>%
summarise(mean_sales = mean(Sales), sd_sales = sd(Sales)  )
View(data_summary)
data_summary <- album_sales %>%
group_by(Genre) %>%
summarise(mean_sales = mean(Sales), sd_sales = sd(Sales), min_sales = min(Sales)  )
View(data_summary)
View(album_sales)
View(album_sales)
View(data_summary)
mean( album_sales$Adverts )
mean_ads <- mean( album_sales$Adverts )
sd_ads <- sd( album_sales$Adverts)
median_ads <- median( album_sales$Adverts )
album_sales$Adverts %>%
table()
hist(album_sales$Adverts)
album_sales$Adverts %>%
round() %>%
table()
album_sales$Adverts %>%
table()
var_ads <- var(album_sales$Adverts)
range <- range(album_sales$Adverts)
iqr <- IQR(album_sales$Adverts)
album_sales %>%
group_by(Genre) %>%
summarise(mean = mean(Adverts), sd = sd(Adverts), median = median(Adverts), iqr = iqr(Adverts)  )
album_sales %>%
group_by(Genre) %>%
summarise(mean = mean(Adverts), sd = sd(Adverts), median = median(Adverts), iqr = IQR(Adverts)  )
album_sales %>%
group_by(Genre) %>%
summarise(mean = mean(Adverts), sd = sd(Adverts), median = median(Adverts), iqr_ads = IQR(Adverts)  )
album_sales %>%
group_by(Genre) %>%
summarise(mean = mean(Adverts), sd = sd(Adverts), median = median(Adverts), iqr = IQR(Adverts)  )
hist(album_sales$Sales)
View(filtered_data)
library(psych)
skew(album_sales$Sales)
kurtosi(album_sales$Sales)
shapiro.test(album_sales$Sales)
quarto add r-wasm/quarto-drop
install.packages("pak")
pak::pak("r-wasm/rwasm")
quarto render
setwd("~/GitHub/intro_to_r/docs/slides")
setwd("~/GitHub/intro_to_r")
library(readr)
practical1_data <- read_csv("docs/practicals/practical1_data.csv")
View(practical1_data)
mean(practical1_data$age)
library(webexercises)
practical2_data <- read.csv("practical2_data.csv")
library(dplyr) #<1>
library(readr)
practical2_data <- read_csv("docs/practicals/practical2_data.csv")
View(practical2_data)
filter(practical2_data, age > 25)
practical2_data |> filter(age > 25)
setwd("~/GitHub/intro_to_r")
# This example uses the sleep dataset, which is a built-in dataset in R that contains data on the effect of two soporific drugs on sleep duration.
# Load the sleep dataset
data(sleep)
# Paired t-test example: Is there a difference in sleep duration between the two drugs?
# Perform the paired t-test
paired_t_test_result <- t.test(sleep$extra ~ sleep$group, paired = TRUE)
# Print the result
paired_t_test_result
# This example uses the sleep dataset, which is a built-in dataset in R that contains data on the effect of two soporific drugs on sleep duration.
# Load the sleep dataset
data(sleep)
# Paired t-test example: Is there a difference in sleep duration between the two drugs?
# Perform the paired t-test
paired_t_test_result <- t.test(sleep$extra ~ sleep$group, paired = TRUE)
# Print the result
paired_t_test_result
# This example uses the mtcars dataset, which is a built-in dataset in R that contains data on various car models.
# Load the mtcars dataset
data(mtcars)
# Independent t-test example: Is there a difference in fuel efficiency between automatic and manual cars?
# the variable am is a binary variable indicating the type of transmission (0 = automatic, 1 = manual)
# Perform the independent t-test
t_test_result <- t.test(mpg ~ am, data = mtcars)
# Print the result
t_test_result
# This example uses the sleep dataset, which is a built-in dataset in R that contains data on the effect of two soporific drugs on sleep duration.
# Load the sleep dataset
data(sleep)
# Wilcoxon signed-rank test example: Is there a difference in sleep duration between the two drugs?
# Perform the Wilcoxon signed-rank test
wilcoxon_test_result <- wilcox.test(sleep$extra ~ sleep$group, paired = TRUE)
# Print the result
wilcoxon_test_result
# This example uses the mtcars dataset, which is a built-in dataset in R that contains data on various car models.
# Load the mtcars dataset
data(mtcars)
# Mann-Whitney U test example: Is there a difference in fuel efficiency between automatic and manual cars?
# Perform the Mann-Whitney U test
mann_whitney_test_result <- wilcox.test(mpg ~ am, data = mtcars)
# Print the result
mann_whitney_test_result
# This example uses the mtcars dataset, which is a built-in dataset in R that contains data on various car models.
# Load the mtcars dataset
data(mtcars)
# Chi-squared test example: Is there an association between the number of cylinders and the type of transmission?
# Create a contingency table
contingency_table <- table(mtcars$cyl, mtcars$am)
# Perform the chi-squared test
chi_squared_test_result <- chisq.test(contingency_table)
# Print the result
chi_squared_test_result
table(mtcars$cyl, mtcars$am)
chi_squared_test_result
# This example uses the mtcars dataset, which is a built-in dataset in R that contains data on various car models.
# Load the mtcars dataset
data(mtcars)
# Correlation example: Is there a relationship between fuel efficiency and horsepower?
# Calculate the correlation coefficient
correlation_result <- cor(mtcars$mpg, mtcars$hp)
# Print the result
correlation_result
# Correlation example: Is the relationship between fuel efficiency and horsepower significant?
# Perform the correlation test
correlation_test_result <- cor.test(mtcars$mpg, mtcars$hp)
# Print the result
correlation_test_result
correlation_result
correlation_result <- cor.test(mtcars$mpg, mtcars$hp)
correlation_result
# This example uses the mtcars dataset, which is a built-in dataset in R that contains data on various car models.
# Load the mtcars dataset
data(mtcars)
# One-way ANOVA example: Is there a difference in fuel efficiency between cars with different numbers of cylinders?
# Perform the ANOVA
anova_result <- aov(mpg ~ cyl, data = mtcars)
# Print the result
summary(anova_result)
# This example uses the mtcars dataset, which is a built-in dataset in R that contains data on various car models.
# Load the mtcars dataset
data(mtcars)
# Factorial ANOVA example: Is there an interaction effect between the number of cylinders and the type of transmission on fuel efficiency?
# Perform the factorial ANOVA
factorial_anova_result <- aov(mpg ~ cyl * am, data = mtcars)
# Print the result
summary(factorial_anova_result)
# Calculate Cohen's d
# Group 1
mean1 <- 10
sd1 <- 2
n1 <- 30
# Group 2
mean2 <- 12
sd2 <- 3
n2 <- 30
# Calculate pooled standard deviation
sp <- sqrt(((n1 - 1) * sd1^2 + (n2 - 1) * sd2^2) / (n1 + n2 - 2))
# Calculate Cohen's d
d <- (mean1 - mean2) / sp
d
# Load the pwr package
library(pwr)
# Calculate the sample size required for a t-test
# using the d value calculated earlier
pwr.t.test(d = d, sig.level = 0.05, power = 0.80)
